const apiKey = process.env.DODO_PAYMENTS_API_KEY; const productId = process.env.DODO_PRODUCT_ID; fetch('https://test.dodopayments.com/checkouts', { method: 'POST', headers: { 'content-type': 'application/json', authorization: 'Bearer ' + apiKey }, body: JSON.stringify({ product_cart: [{ product_id: productId, quantity: 1 }], return_url: 'http://localhost' }) }).then(r=>r.json()).then(console.log).catch(console.error);
